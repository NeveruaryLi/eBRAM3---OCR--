# 使用 ebram conda 环境运行（Python 3.11）
# conda activate ebram
# pip install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple
# 安装后还需执行一次：playwright install chromium

# ── Web 框架 ──────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.24
httpx>=0.27.0
python-dotenv>=1.0.0

# ── PDF / 文档处理 ────────────────────────────────────────
pymupdf>=1.23.0          # PDF/OCR 预处理、Case 4 页面翻译与 Case 6B 图片材料封装
python-docx>=1.1.0       # Case 1/2/5 报告及 Case 6B 模板解析、精准回填
docx2pdf>=0.1.8          # Word→PDF 转换（Windows 需安装 Microsoft Word）
openpyxl>=3.1.0          # Case 6B：本地读取 XLSX 材料
lxml>=5.0.0              # Case 6A：官网知识库 HTML 清洗；Case 6B：DOCX OOXML 检查

# ── Case 5 爬虫 ───────────────────────────────────────────
playwright>=1.40.0       # HKLII 抓取；安装后需 playwright install chromium
